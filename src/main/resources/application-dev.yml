spring:
  jpa:
    show-sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:mem:${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.h2.Driver

api:
  version: v1

spring-doc:
  api-docs:
    path: /${api.version}/product-json # http://{host}:{port}/{api.version}/custom-docs
  swagger-ui:
    path: /${api.version}/product/ # http://{host}:{port}/{api.version}/custom-swagger/swagger-ui/index.html

logging:
  level:
    org.springframework.security: debug

server:
  port: ${SERVER_PORT}